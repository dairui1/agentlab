# AgentLab Web 设计约定

本文件约束 `apps/agent-history/public` 下的所有页面，包括带查询参数的专题详情和机制工作台。设计改动应同时检查这些入口，不能以首页截图代替全站验收。

## 定位

AgentLab 是面向 Agent 开发者的研究工作台。用户主要做三件事：浏览变化、比较实现、追溯证据。界面应安静、清晰、适合长时间阅读；不做营销首页，不用装饰代替信息组织。

保留现有品牌、Agent 图标、研究结论、证据编号、版本范围、原始链接和交互。官方事实、本地推断、工程建议、未知项不能因为简化排版而混为一谈。

## 共用基础

- 颜色以 `styles.css` 为唯一基础：`--page`、`--surface`、`--surface-raised`、`--surface-muted`、`--text`、`--text-soft`、`--muted`、`--border`、`--accent`。
- 亮色使用白色与中性灰；深色使用中性黑灰。专题局部变量应引用基础变量，不另造一套带色底板。
- 绿色用于当前操作、有效状态；蓝色用于比较或推断；红色用于错误和删除；琥珀色用于警告与边界。产品配色只能用于图例和必要的对照标记。
- 保留现有系统中文字体与等宽字体。正文不设置负字距，不随视口宽度缩放字号。
- 列表摘要和说明正文通常为 14px，长文章正文为 16px；辅助文字通常为 12px，证据编号等紧凑标记不小于 11px。
- 工具页 H1 为 28px；长文章 H1 为 32px，手机 26px。H2 通常为 24px。标题与正文均保留稳定行高，长术语允许换行。

## 页面结构

### 导航与入口

`site-navigation.js` 中的 `<agentlab-navigation>` 是唯一全站导航实现。当前项用底部线条标识，不把导航做成悬浮按钮组。专题菜单保留全部入口和“全部专题”，支持方向键、Home/End、Escape 及焦点离开时关闭。

### 更新情报与版本比较

桌面列表将 Agent 和版本放在左栏，标题与摘要放在右栏；手机按阅读顺序堆叠。变化指标、来源与解读归属分层显示。“查看证据”保持稳定位置，整行保留原比较链接。按天分组后不重复显示每条日期。

比较页保留版本选择、交换、变化统计、分析、事实证据、Monaco 差异、结构导航、换行和原有空态。不能用不可交互的展示组件替换这些控件。

### 专题目录与证据详情

目录提供直接可用的搜索和筛选。详情按“问题与版本范围、工程判断、边界、关键证据、完整证据”组织。使用分隔线与留白，不把每一节包进卡片。展开的证据内容嵌入文档流，保留定位、来源与未知项。

### 长文章与交互研究

文章采用统一的标题、目录、正文、表格和证据抽屉。目录与阅读进度是定位工具，不与正文争夺视觉注意力。长表格可以在明确的局部容器内横向滚动，页面本身不得横向溢出。

Goal、CUA、TokenBudget 的场景控件、回放、状态图和实验日志属于真实研究功能，不作为装饰删除。DSH 和 Grok 的特有证据结构可以保留，但页面底色、字体层级、按钮和导航遵循共用约定。

### 机制工作台

七个机制共用筛选、五种视图和证据检查器。密度来自对齐和明确分组，而不是缩小文字。移动端保留视图切换和证据入口，抽屉关闭后恢复原焦点。

## 控件与状态

- 工具动作使用已有 Lucide 图标和可访问名称；明确命令可以使用文字按钮。二元选择使用开关或复选框，选项集合使用菜单或原生选择器。
- 圆角不超过 8px；仅弹层、单个重复条目和真正独立的工具可以有边框或投影。避免卡片套卡片、渐变、发光和无意义图形。
- 手机主要点击目标至少 44px；焦点轮廓必须可见。操作后不能发生意外尺寸变化。
- 加载、无结果、失败必须区分。请求失败后停止加载提示，保留错误原因和重试路径。
- 保留 `prefers-color-scheme` 与 `prefers-reduced-motion`。不要把深色模式下的浅色文字放到局部硬编码白底上。

## 文案与图像

使用自然中文，保留必要的 Prompt、Tool、Runtime 等技术术语。删除页面自夸、功能说明式标语和重复标签，但不删除事实边界。研究摘要不因视觉改版自动改写；长内容完整保留在详情中。

图像只用于 Agent 身份、产品证据与研究解释。复用已有资源，声明稳定宽高和合适的替代文本；不生成无关配图。原生图表、回放和代码控件仍使用代码实现。

## 变更验收

1. 从 HTML、`research-index.json` 和机制注册表枚举全部入口，不能只数 HTML 文件。
2. 每个入口检查载入结果、标题、所有正文段落所在区域、页尾、链接与资源；动态页按模板检查筛选、切换、证据展开、抽屉和恢复。
3. 至少检查 1440px 桌面、390px 手机；共享布局补查 320px 和 1024px。首屏、中部、底部都要有证据，长页保留完整截图或滚动记录。
4. 检查页面横向溢出、文字截断、局部滚动容器、遮挡、空态、错误态、深色和减少动画。
5. 运行 `npm test`、`npm run build`、`python3 scripts/verify_deploy.py` 和 `git diff --check`，命令在 `apps/agent-history` 下执行。
6. 在走查报告中列出真实覆盖范围、截图、已修问题和剩余限制。截图评分是主观反馈，不能代替功能验收。

用 `node scripts/ui_audit_routes.mjs` 生成入口清单。`tests/design-system.test.js` 会核对 HTML、研究索引及机制注册表，新增页面不得绕过清单。

最近一次全站走查见 [DESIGN-REVIEW.md](DESIGN-REVIEW.md)。

未经用户要求，不安装依赖，不修改研究数据，不提交或发布。
