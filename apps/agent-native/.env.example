# AgentLab's first Agent-Native app is action-first and can run local CLI
# actions without a database. Add a SQL connection when enabling persistent
# runtime state, jobs, auth, or UI surfaces.
DATABASE_URL=

