# 参与 AgentLab

感谢你愿意帮助 AgentLab。这个项目同时处理代码、公开来源和模型分析，因此贡献不仅要能运行，也要可追溯。

## 适合提交的内容

- 修正错误事实、失效链接或版本归属。
- 增加官方文档、公开仓库、release note 等一手来源。
- 改进同步、规范化、分析、测试和发布脚本。
- 改进更新情报、筛选和版本比较体验。
- 补充可复现的数据解析和边界测试。

涉及数据格式变化、新来源类型或大范围界面调整时，建议先开 Issue 说明问题和方案。

## 数据与研究要求

1. 优先引用官方文档、公开仓库、官方 release 或可复现实验。
2. 明确区分来源事实、产品观察、工程推断和待验证问题。
3. 不提交私有 Prompt、Token、内部日志、账号导出或未授权泄露内容。
4. 引用第三方内容时以摘要和结构化结论为主，不复制不必要的长篇原文。
5. 易变化的事实需要记录来源和版本或访问日期。

## 本地校验

需要 Node.js 22 或更高版本、Python 3.11 或更高版本：

```bash
cd apps/agent-history
npm ci
npm test
```

涉及完整数据构建时再运行：

```bash
npm run sync
npm run build
```

`npm run sync` 会访问公开上游。UI 改动应在 `npm run dev` 后检查桌面端和移动端布局。

## Pull Request

- 一个 PR 尽量解决一个明确问题。
- 生成数据和构建产物不要提交到仓库。
- 新增行为应补充聚焦的测试。
- UI 改动应说明实际检查的视口。
- 提交说明应列出实际运行的校验，以及因环境限制未运行的检查。
- 不要顺手重写与当前问题无关的内容。
